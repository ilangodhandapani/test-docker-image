name: Build/run docker image
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          docker build -t testimage .
          docker run -dit --name test-cont -p 80:80 testimage
          curl "http://localhost"
