name: Build/run docker image
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          docker build -t testimage .
          docker run -it --rm -d -p 8080:80 --name web testimage
          curl "http://localhost:8080"
